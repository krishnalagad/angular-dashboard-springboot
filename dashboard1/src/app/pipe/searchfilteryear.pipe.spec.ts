import { SearchfilteryearPipe } from './searchfilteryear.pipe';

describe('SearchfilteryearPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilteryearPipe();
    expect(pipe).toBeTruthy();
  });
});
