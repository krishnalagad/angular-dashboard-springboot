import { SearchfilterregionPipe } from './searchfilterregion.pipe';

describe('SearchfilterregionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilterregionPipe();
    expect(pipe).toBeTruthy();
  });
});
