import { SearchfiltertopicPipe } from './searchfiltertopic.pipe';

describe('SearchfiltertopicPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfiltertopicPipe();
    expect(pipe).toBeTruthy();
  });
});
