import { SearchfiltercityPipe } from './searchfiltercity.pipe';

describe('SearchfiltercityPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfiltercityPipe();
    expect(pipe).toBeTruthy();
  });
});
